

chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
    // TODO: add content.js to webpack.config.js 
    // Find way to return content from this function back to Binder.vue file 
    console.log("it works wuhu")
    //document.getElementsByClassName({})
})